# CMakeLists.txt in Repo 1
cmake_minimum_required(VERSION 3.10)
project(Repo1)

# Add Repo 2 as a subdirectory
add_subdirectory(Repo2)

# Here, you can define any additional files for Repo 1 if needed
